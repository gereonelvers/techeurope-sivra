# Core supervisor slice — light, fast to install, no GPU/ML needed.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
pyyaml>=6.0
httpx>=0.27
python-dotenv>=1.0

# Frontier eval + Pioneer (OpenAI-compatible) client
openai>=1.40

# Feedback->retrain loop A: read resolved Escalation rows from Postgres
# (pioneer/feedback_dataset.py). Public Railway proxy URL in .env DATABASE_URL.
psycopg[binary]>=3.1

# Optional ML / heavy deps live in requirements-ml.txt:
#   gliner2[local]   (guardrail; CPU)   — pip install 'gliner2[local]'
#   openai anthropic google-genai       (eval-vs-frontier)
#   unsloth trl peft vllm transformers  (buyer-agent fine-tune; GPU/Modal)
