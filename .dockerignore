apps/
agent/
ui/
demo/
.venv/
**/node_modules/
**/__pycache__/
**/*.pyc
.git/
.env
*.md
# NOTE: pioneer/, data/, scripts/ are intentionally NOT ignored: the
# retrain_cron image (retrain_cron/Dockerfile, built from the repo root) COPYs
# pioneer/ + data/datasets/. Other images (supervisor, etc.) only COPY their own
# dirs, so keeping these in the context doesn't affect them.
